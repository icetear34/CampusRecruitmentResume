以下內容可直接交付給 agent 實作。
目標是建立一個**全新、獨立、與現有項目無直接關聯**的 API 服務：

**服務名稱：`CampusRecruitmentResumeService`**

---

# CampusRecruitmentResumeService 實作交付規格

## 1. 任務目標

建立一個獨立的 ASP.NET Core Web API 服務，用於接收校徵專用履歷表單資料，並寫入 MSSQL 資料庫。

此服務特性如下：

* 與現有 `iRecruit`、`iRecruitApi`、其他既有網站/模組**無直接程式相依**
* 僅作為**收件 API**
* 不提供前端查詢、更新、刪除功能
* 不處理附件上傳
* 以 IIS 子路徑方式部署
* 前端靜態網站以 JS `fetch` 呼叫此 API
* 使用 SQL 帳密連線至 MSSQL

---

# 2. 系統命名與部署定位

## 2.1 服務名稱

* Solution / Project Name: `CampusRecruitmentResumeService`

## 2.2 API 路徑

採同網域子路徑部署，建議：

```text
/CampusRecruitmentResumeService/api/job-applications
```

## 2.3 部署位置

* IIS Server: `10.35.0.10`
* DB Server: `10.35.1.154`

## 2.4 與既有系統關係

本服務為**新服務**，不得直接引用現有專案程式碼、DTO、Entity、DbContext、Shared Library，除非明確確認為共用基礎設施且不造成耦合。

---

# 3. 技術要求

## 3.1 後端技術

使用：

* `.NET 8`
* `ASP.NET Core Web API`
* `Entity Framework Core` + `SqlServer Provider`

## 3.2 部署模式

IIS Application + ASP.NET Core Hosting Bundle

## 3.3 IIS Application Pool

* Name: `CampusRecruitmentResumeService`
* .NET CLR: `No Managed Code`
* Pipeline: `Integrated`
* Start Mode: `AlwaysRunning`
* 32-bit: `False`

---

# 4. 功能範圍

## 4.1 本期只做以下功能

1. 接收應徵資料
2. 驗證欄位
3. 寫入 MSSQL
4. 回傳成功 / 失敗結果

## 4.2 明確不做

1. 查詢履歷資料 API
2. 修改履歷資料 API
3. 刪除履歷資料 API
4. 檔案上傳
5. 後台畫面
6. 驗證碼整合
7. 郵件通知
8. 與既有 HR / 招募系統整合

---

# 5. API 規格

## 5.1 Endpoint

```http
POST /api/job-applications
```

掛在 IIS 子路徑後，實際呼叫路徑為：

```http
POST /CampusRecruitmentResumeService/api/job-applications
```

## 5.2 Request Content-Type

```http
application/json
```

## 5.3 Request Model

```json
{
  "name": "王小明",
  "appliedPosition": "系統需求分析工程師",
  "birthDate": "1995-08-10",
  "mobilePhone": "0912345678",
  "email": "test@example.com",
  "contactAddress": "新竹市東區xxxx",
  "educations": [
    {
      "schoolName": "某某大學",
      "departmentName": "資訊管理學系",
      "graduationDate": "2018-06-30",
      "degreeName": "學士"
    }
  ],
  "certificates": [
    {
      "certificateDate": "2020-05-01",
      "certificateName": "TOEIC 800"
    }
  ],
  "experiences": [
    {
      "companyName": "ABC Tech",
      "startYear": 2021,
      "startMonth": 3,
      "endYear": 2024,
      "endMonth": 12,
      "jobDescription": "負責後端 API 與系統整合",
      "jobTitle": "Backend Engineer"
    }
  ],
  "languageSkills": {
    "englishLevel": "Normal",
    "japaneseLevel": "Basic"
  },
  "remark": "熟悉 C#、.NET、SQL Server、JavaScript"
}
```

## 5.4 成功 Response

```json
{
  "success": true,
  "applicationId": "1f4c6d7c-7b50-4c1d-b5d5-6ef6ecf1d829",
  "message": "Application received."
}
```

## 5.5 驗證失敗 Response

```json
{
  "success": false,
  "message": "Validation failed.",
  "errors": {
    "name": ["Name is required."],
    "mobilePhone": ["Invalid format."]
  }
}
```

## 5.6 系統錯誤 Response

```json
{
  "success": false,
  "message": "System error."
}
```

---

# 6. 欄位規則

## 6.1 必填欄位

* `name`
* `appliedPosition`
* `mobilePhone`

## 6.2 選填欄位

* `birthDate`
* `email`
* `contactAddress`
* `educations`
* `certificates`
* `experiences`
* `languageSkills`
* `remark`

## 6.3 格式驗證

* `email` 必須為合法 email 格式
* `birthDate` 不可大於今天
* `startMonth` / `endMonth` 必須為 1~12
* `startYear` / `endYear` 建議限制 1900~2100

## 6.4 長度限制

* `name`: 100
* `appliedPosition`: 100
* `mobilePhone`: 30
* `email`: 255
* `contactAddress`: 255
* `schoolName`: 150
* `departmentName`: 150
* `degreeName`: 50
* `certificateName`: 150
* `companyName`: 150
* `jobTitle`: 100

## 6.5 語言等級固定值

只接受：

* `Advanced`
* `Normal`
* `Basic`

---

# 7. 資料庫設計

資料庫：MSSQL
不得將所有資料硬塞成單表，必須拆主表與子表。

## 7.1 主表 `JOB_APPLICATION`

```sql
CREATE TABLE dbo.JOB_APPLICATION (
    ID UNIQUEIDENTIFIER NOT NULL PRIMARY KEY,
    NAME NVARCHAR(100) NOT NULL,
    APPLIED_POSITION NVARCHAR(100) NOT NULL,
    BIRTH_DATE DATE NULL,
    MOBILE_PHONE NVARCHAR(30) NOT NULL,
    EMAIL NVARCHAR(255) NULL,
    CONTACT_ADDRESS NVARCHAR(255) NULL,
    ENGLISH_LEVEL NVARCHAR(20) NULL,
    JAPANESE_LEVEL NVARCHAR(20) NULL,
    REMARK NVARCHAR(MAX) NULL,
    CREATED_AT DATETIME2 NOT NULL CONSTRAINT DF_JOB_APPLICATION_CREATED_AT DEFAULT SYSUTCDATETIME(),
    CREATED_IP NVARCHAR(64) NULL,
    USER_AGENT NVARCHAR(500) NULL
);
```

## 7.2 教育表 `JOB_APPLICATION_EDUCATION`

```sql
CREATE TABLE dbo.JOB_APPLICATION_EDUCATION (
    ID UNIQUEIDENTIFIER NOT NULL PRIMARY KEY,
    APPLICATION_ID UNIQUEIDENTIFIER NOT NULL,
    SCHOOL_NAME NVARCHAR(150) NOT NULL,
    DEPARTMENT_NAME NVARCHAR(150) NULL,
    GRADUATION_DATE DATE NULL,
    DEGREE_NAME NVARCHAR(50) NULL,
    SORT_ORDER INT NOT NULL,
    CONSTRAINT FK_JOB_APPLICATION_EDUCATION_APPLICATION
        FOREIGN KEY (APPLICATION_ID) REFERENCES dbo.JOB_APPLICATION(ID)
);
```

## 7.3 證照表 `JOB_APPLICATION_CERTIFICATE`

```sql
CREATE TABLE dbo.JOB_APPLICATION_CERTIFICATE (
    ID UNIQUEIDENTIFIER NOT NULL PRIMARY KEY,
    APPLICATION_ID UNIQUEIDENTIFIER NOT NULL,
    CERTIFICATE_DATE DATE NULL,
    CERTIFICATE_NAME NVARCHAR(150) NOT NULL,
    SORT_ORDER INT NOT NULL,
    CONSTRAINT FK_JOB_APPLICATION_CERTIFICATE_APPLICATION
        FOREIGN KEY (APPLICATION_ID) REFERENCES dbo.JOB_APPLICATION(ID)
);
```

## 7.4 經歷表 `JOB_APPLICATION_EXPERIENCE`

```sql
CREATE TABLE dbo.JOB_APPLICATION_EXPERIENCE (
    ID UNIQUEIDENTIFIER NOT NULL PRIMARY KEY,
    APPLICATION_ID UNIQUEIDENTIFIER NOT NULL,
    COMPANY_NAME NVARCHAR(150) NOT NULL,
    START_YEAR INT NULL,
    START_MONTH INT NULL,
    END_YEAR INT NULL,
    END_MONTH INT NULL,
    JOB_DESCRIPTION NVARCHAR(MAX) NULL,
    JOB_TITLE NVARCHAR(100) NULL,
    SORT_ORDER INT NOT NULL,
    CONSTRAINT FK_JOB_APPLICATION_EXPERIENCE_APPLICATION
        FOREIGN KEY (APPLICATION_ID) REFERENCES dbo.JOB_APPLICATION(ID)
);
```

## 7.5 索引

```sql
CREATE INDEX IX_JOB_APPLICATION_CREATED_AT
ON dbo.JOB_APPLICATION(CREATED_AT);

CREATE INDEX IX_JOB_APPLICATION_EMAIL
ON dbo.JOB_APPLICATION(EMAIL);

CREATE INDEX IX_JOB_APPLICATION_MOBILE_PHONE
ON dbo.JOB_APPLICATION(MOBILE_PHONE);
```

---

# 8. 專案結構要求

```text
CampusRecruitmentResumeService
├─ Controllers
│  └─ JobApplicationsController.cs
├─ Models
│  ├─ Requests
│  │  ├─ JobApplicationCreateRequest.cs
│  │  ├─ EducationItemRequest.cs
│  │  ├─ CertificateItemRequest.cs
│  │  ├─ ExperienceItemRequest.cs
│  │  └─ LanguageSkillsRequest.cs
│  └─ Responses
│     └─ ApiResult.cs
├─ Data
│  ├─ CampusRecruitmentResumeDbContext.cs
│  └─ Entities
│     ├─ JobApplicationEntity.cs
│     ├─ JobApplicationEducationEntity.cs
│     ├─ JobApplicationCertificateEntity.cs
│     └─ JobApplicationExperienceEntity.cs
├─ Services
│  ├─ Interfaces
│  │  └─ IJobApplicationService.cs
│  └─ JobApplicationService.cs
├─ Validators
├─ appsettings.json
├─ Program.cs
└─ web.config
```

---

# 9. DTO 定義要求

Agent 必須建立以下 request models：

* `JobApplicationCreateRequest`
* `EducationItemRequest`
* `CertificateItemRequest`
* `ExperienceItemRequest`
* `LanguageSkillsRequest`

並建立共用 response model，例如：

* `ApiResult`
* `ApiResult<T>`

---

# 10. 服務層要求

必須將寫入邏輯放在 service，不可把全部商業邏輯塞進 controller。

## 10.1 Service 流程

1. 驗證 request
2. 建立 `applicationId`
3. 建立主表 entity
4. 建立 education/certificate/experience 子表 entities
5. 寫入 DB
6. 交易提交
7. 回傳結果

## 10.2 Transaction

必須使用單一 transaction。
若任一明細寫入失敗，整筆 rollback。

---

# 11. Controller 要求

## 11.1 路由

```csharp
[ApiController]
[Route("api/job-applications")]
```

## 11.2 功能

只保留：

* `POST Create`

不得額外建立查詢、修改、刪除 endpoint。

---

# 12. EF Core 要求

## 12.1 DbContext 名稱

`CampusRecruitmentResumeDbContext`

## 12.2 Mapping 要求

* 明確設定 table name
* 明確設定 column max length
* 明確設定 relationship
* 不依賴隱式約定完成全部 mapping

---

# 13. 設定檔要求

## 13.1 `appsettings.json`

需包含 connection string key：

```json
{
  "ConnectionStrings": {
    "CampusRecruitmentResumeDb": "Server=10.35.1.154;Database=RecruitDB;User Id=xxx;Password=xxx;TrustServerCertificate=True;MultipleActiveResultSets=True"
  }
}
```

實作時不要把真實密碼硬編碼在原始碼。
部署版可改用環境變數或伺服器覆蓋設定。

---

# 14. IIS 部署要求

## 14.1 部署型態

於 `Default Web Site` 下建立新的 IIS Application：

```text
/CampusRecruitmentResumeService
```

## 14.2 實體路徑

例如：

```text
D:\Sites\CampusRecruitmentResumeService\
```

## 14.3 App Pool

建立專用 pool：

* Pool Name: `CampusRecruitmentResumeService`
* CLR: `No Managed Code`
* Pipeline: `Integrated`

## 14.4 伺服器前置條件

IIS Server 必須已安裝：

* .NET 8 Runtime
* ASP.NET Core Hosting Bundle

---

# 15. 網路與資料庫連線前提

這部分不是程式碼可自行解決，agent 只需列入部署檢查：

## 15.1 必要條件

`10.35.0.10` 必須可連至：

```text
10.35.1.154:1433
```

## 15.2 檢查方式

在 API Server 執行：

```powershell
Test-NetConnection 10.35.1.154 -Port 1433
```

## 15.3 若失敗

需由基礎設施/網管確認：

* VLAN / ACL / 路由
* Windows Firewall
* SQL Server TCP/IP
* SQL Login 權限

---

# 16. 安全與限制

## 16.1 必做

* Server-side validation
* 不回傳內部 exception detail
* 記錄 `CREATED_IP`
* 記錄 `USER_AGENT`
* 限制 request body 大小
* 僅接受 JSON POST

## 16.2 本期不做

* JWT
* OAuth
* CAPTCHA
* Audit trail
* IP 白名單後台管理

---

# 17. 前端串接假設

前端將以靜態網站 JavaScript 呼叫：

```javascript
fetch('/CampusRecruitmentResumeService/api/job-applications', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify(payload)
})
```

因此 agent 必須確保：

* API 路由與 IIS 子路徑一致
* 不要求額外認證 header
* 可正常接受 JSON body

---

# 18. 驗收標準

以下條件全部成立才算完成：

## 18.1 API

* 能成功接受合法 JSON
* 能寫入主表與所有子表
* 能回傳 `success=true` 與 `applicationId`

## 18.2 驗證

* 缺 `name`、`appliedPosition`、`mobilePhone` 時回 400
* email 格式錯誤時回 400
* month 非 1~12 時回 400

## 18.3 DB

* `JOB_APPLICATION`
* `JOB_APPLICATION_EDUCATION`
* `JOB_APPLICATION_CERTIFICATE`
* `JOB_APPLICATION_EXPERIENCE`

四表結構正確，FK 正常

## 18.4 部署

* IIS Application 可啟動
* 經由 IIS 子路徑可成功呼叫
* API Server 能連到 MSSQL

---

# 19. Agent 交付物

agent 最終需產出以下內容：

## 19.1 程式碼

* 完整 .NET 8 Web API 專案
* Controller
* DTO
* Entity
* DbContext
* Service
* Validation
* Exception handling

## 19.2 SQL

* 建表 SQL 腳本
* 索引 SQL 腳本

## 19.3 部署檔

* `appsettings.json` 範本
* `web.config`
* 發佈說明

## 19.4 文件

* API request/response 範例
* IIS 部署步驟
* DB 連線檢查說明

---

# 20. Agent 執行限制

1. 不得直接耦合現有 `iRecruitApi`
2. 不得假設可重用現有 DB schema
3. 不得新增未要求功能
4. 不得將教育/證照/經歷改存成單一 JSON 欄位取代正規化表
5. 不得略過 transaction
6. 不得把主要邏輯全部塞進 controller

